packages:
  - 'packages/*'
  - 'examples/hello-world/frontend'

# packages/create-atrium-host has no build step (plain ESM JS) and is
# picked up via `packages/*` above. Its test suite uses Node's built-in
# test runner — no vitest dep — so it stays out of the workspace's
# pooled dev tooling.
